# === FRONTEND CONFIG ===
frontend:
  replicaCount: 2
  image:
    repository: lilygniedz/orbital-frontend
    tag: latest
    pullPolicy: IfNotPresent

  service:
    type: ClusterIP
    port: 80

  livenessProbe:
    httpGet:
      path: /
      port: http
    initialDelaySeconds: 10
    periodSeconds: 30
  readinessProbe:
    httpGet:
      path: /
      port: http
    initialDelaySeconds: 5
    periodSeconds: 10

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 250m
      memory: 128Mi

  nodeSelector: {}
  tolerations: []
  affinity: {}

# === BACKEND CONFIG ===
backend:
  replicaCount: 2
  image:
    repository: lilygniedz/satellite-backend
    tag: latest
    pullPolicy: IfNotPresent

  service:
    type: ClusterIP
    port: 5000

  livenessProbe:
    httpGet:
      path: /api/health
      port: 5000
    initialDelaySeconds: 10
    periodSeconds: 30
  readinessProbe:
    httpGet:
      path: /api/health
      port: 5000
    initialDelaySeconds: 5
    periodSeconds: 10

  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi

  nodeSelector: {}
  tolerations: []
  affinity: {}

# === TiDB Secrets  ===
tidb:
  host: "basic-tidb.tidb-cluster.svc.cluster.local"
  root: "root"
  password: ""
  database: "satellite_sim"

tidbCAConfigMapName: tidb-ca-configmap

# === INGRESS CONFIG (PRODUCTION) ===
ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
  hosts:
    - host: orbital-alt.local
      paths:
        - path: /()(.*)
          pathType: ImplementationSpecific
          backend:
            serviceName: satellite-app-frontend
            servicePort: 80
        - path: /api(/|$)(.*)
          pathType: ImplementationSpecific
          backend:
            serviceName: satellite-app-backend
            servicePort: 5000
serviceAccount:
  create: true
  name: ""
  automount: true
  annotations: {}


autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
